<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="item"
            type="com.mobilehealthsports.vaccinepass.business.models.User" />

        <variable
            name="itemClick"
            type="com.mobilehealthsports.vaccinepass.ui.user_select.SelectUserViewModel.ItemClickListener"/>
    </data>

<androidx.cardview.widget.CardView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:padding="20dp"
    android:layout_marginHorizontal="20dp"
    android:layout_marginVertical="5dp"
    app:cardElevation="15dp"
    app:cardCornerRadius="15dp"
    android:onClick="@{() -> itemClick.onItemClicked(item)}">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.cardview.widget.CardView
            android:id="@+id/list_item_user_picture"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_margin="15dp"
            android:innerRadius="0dp"
            android:shape="ring"
            app:cardBackgroundColor="@color/app_primary"
            app:cardCornerRadius="@dimen/activity_select_user_usr_img_radius"
            app:cardElevation="15dp">

            <ImageView
                android:layout_width="@dimen/activity_select_user_usr_img_diameter"
                android:layout_height="@dimen/activity_select_user_usr_img_diameter"
                android:contentDescription="@string/activity_select_user_pic_description"
                android:src="@drawable/img_user"/>
        </androidx.cardview.widget.CardView>

        <TextView
            android:id="@+id/list_item_user_first_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{item.firstName}"
            android:layout_marginStart="15dp"
            android:textSize="24sp"
            app:layout_constraintStart_toEndOf="@id/list_item_user_picture"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            />

        <TextView
            android:id="@+id/list_item_user_last_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{item.lastName}"
            android:textSize="24sp"
            android:layout_marginStart="5dp"
            app:layout_constraintStart_toEndOf="@id/list_item_user_first_name"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>
</layout>