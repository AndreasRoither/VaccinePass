{
  "name": "validation-server",
  "version": "0.0.1",
  "description": "Checks if the client is authenticated. If so the vaccination string will be signed and sent back as a response.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon -L src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "VaccinePassGroup",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "crypto": "^1.0.1",
    "express": "^4.17.1",
    "jsencrypt": "^3.0.0-rc.1",
    "nodemon": "^2.0.6",
    "pg": "^8.5.1"
  }
}
